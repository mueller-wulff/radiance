<% if @page.next_page %>
	<%= link_to ">>", student_stitch_unit_page_path(@page.next_page.stitch_unit, @page.next_page ) %>
<% else %>
	<p><%= link_to "Back to Course", student_course_path(@page.course) %></p>
<% end %>