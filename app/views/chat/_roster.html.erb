<div id="roster">
  <p>Roster</p>

  <div class="container">
    <% roster_elements.each do |el| %>
      <div class="element <%=el[:type]%>" id="<%=el[:channel_id]%>"><%=el[:name]%></div>
    <%end%>
  </div>
</div>
