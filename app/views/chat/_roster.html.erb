<div id="roster">
  <p>Roster</p>

  <div class="container">
    <% roster_elements.each do |el| %>
      <a href="/chat/channel_for/<%=el[:channel_id]%>">
        <div class="element <%=el[:type]%>" id="<%=el[:channel_id]%>">
          <%=el[:name]%>
        </div>
      </a>
    <%end%>
  </div>
</div>
