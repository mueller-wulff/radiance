<% if @profile.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

    <ul>
      <% @profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= f.fields_for :profile do |g| %>
  <p class="input">
    <%= label :login, t(:login, :scope => :profile) %>  
    <%= g.text_field :login %>  
  </p>  
  <p class="input">
    <%= label :name, t(:name, :scope => :profile) %>  
    <%= g.text_field :name %>  
  </p>  
  <p class="input">  
    <%= label :lastname, t(:lastname, :scope => :profile) %>  
    <%= g.text_field :lastname %>  
  </p>  
  <p class="input">
    <%= label :email, t(:email, :scope => :profile) %>  
    <%= g.text_field  :email %>  
  </p> 
<% end %>
<p class="select">
	<%= f.label :courses %>
	<ol>		
		<% @courses.each do |course| %>
			<li><%= check_box_tag("tutor[course_ids][]", course.id, @tutor.courses.include?(course)) %> <%= course.short_title %></li>
		<% end %>
	</ol>
</p>
<p class="submit"> 
  <%= submit_tag t(:submit, :scope => :general) %>  
</p>