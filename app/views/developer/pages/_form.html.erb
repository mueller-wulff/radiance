<%= form_for([@stitch_module, @page]) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, t(:title, :scope => :general) %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :stitch_module_id, t(:stitch_module, :scope => :stitch_module)+" "+t(:id, :scope => :general) %><br />
    <%= f.text_field :stitch_module_id %>
  </div>
  <div class="actions">
    <%= f.submit t(:submit, :scope => :general) %>
  </div>
<% end %>
